#ifndef GRAPHICSLOGINITEM_H
#define GRAPHICSLOGINITEM_H

#include <QGraphicsItem>
#include <QTimeLine>
/*
class GraphicsLoginItem : public QObject, public QGraphicsItem
{
    Q_OBJECT
public:
    GraphicsLoginItem(QGraphicsItem *parent = 0);
    ~GraphicsLoginItem();
    QRectF boundingRect() const;

signals:


public slots:

protected:
    void paint(QPainter *painter, const QStyleOptionGraphicsItem *, QWidget *);
    //void keyPressEvent(QKeyEvent *event);

private:
    QTimeLine *_timeline;
};
*/
#endif // GRAPHICSLOGINITEM_H
